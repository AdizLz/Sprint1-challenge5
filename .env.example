# .env.example - plantilla de variables de entorno (NO incluir secretos en el repositorio)
# Copia este archivo a .env y ajusta valores locales si lo necesitas.

# Perfil activo por defecto (dev/test/prod). En producción normalmente el despliegue define esto.
# SPRING_PROFILES_ACTIVE=dev

# Puerto del servidor (opcional)
# SERVER_PORT=8080

# Variables para la base de datos (para production, ej. PostgreSQL)
# SPRING_DATASOURCE_URL=jdbc:postgresql://db-host:5432/tienda_prod
# SPRING_DATASOURCE_USERNAME=tienda
# SPRING_DATASOURCE_PASSWORD=supersecreto

# Otras variables útiles
# LOG_LEVEL=INFO

